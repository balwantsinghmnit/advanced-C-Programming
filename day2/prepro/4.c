#include <stdio.h>
#include "data.h"
#define X(w) w*w

void main()
{
	int varx = 100;
	int vary = 0;
	varx = varx + max_temp + TEMP + QEMP;
	vary = X(4) + X(varx);
	printf("vary = %d\n",vary);
	printf("varx = %d\n",varx);
	return;
}